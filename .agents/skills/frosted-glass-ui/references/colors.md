# Frosted Glass Colors

Colors are defined in `src/app/styles/frosted-glass.css`.
Do not prompt for colors.

- **Primary**: Cyan / Sky Blue (`var(--primary-500)`)
- **Secondary**: Indigo (`var(--secondary-500)`)
- **Text**: `var(--on-surface)` and `var(--on-surface-variant)`
