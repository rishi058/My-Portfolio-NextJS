# Neumorphic Typography

Use the typography defined in `src/app/styles/neumorphic.css`.

- **Font Family**: `var(--font-sans)` (`Inter`)
- **Accent Text**: `var(--primary-text)`
- **Body Text**: `var(--on-surface-variant)`

Maintain readability by ensuring text contrast against the neutral gray or slate backgrounds.
